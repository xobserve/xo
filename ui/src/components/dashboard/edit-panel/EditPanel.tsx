import { Panel } from "types/dashboard"

interface EditPanelProps {
    panel: Panel
    onChange: any
    onDiscard: any
}

const EditPanel = ({panel,onChange, onDiscard}: EditPanelProps) => {
    return (<>
    </>)
}

export default EditPanel