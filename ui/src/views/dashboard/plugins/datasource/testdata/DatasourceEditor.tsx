import { Datasource } from "types/datasource"

interface Props {
    datasource: Datasource
    onChange: any
}

const TestDataDatasourceEditor = ({datasource, onChange}: Props) => {
    return (<></>)
}

export default TestDataDatasourceEditor
