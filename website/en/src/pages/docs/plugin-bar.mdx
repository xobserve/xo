---
title: Bar chart
description:
---

import TextList  from '@/components/docs/TextList'
import Code  from '@/components/docs/Code'

Although we can use the `Bar` type of `Graph` panel, but if you need more powerful bar chart, you should choose `Bar` panel:

<img src={require('@/img/docs/plugin/bar-chart1.jpg').default.src} alt="bar-chart1" width={3086} height={1658} className="rounded-lg ring-1 ring-black/5" />
