---
title: Stat 
description:
---

import TextList  from '@/components/docs/TextList'
import Code  from '@/components/docs/Code'

你可以认为 `Stat` 图表是一个 `Big Value` + 简化版的 `Graph` 图表。

<img src={require('@/img/docs/plugin/stat.jpg').default.src} alt="stat"  style={{height: '300px'}} className="rounded-lg ring-1 ring-black/5" />
